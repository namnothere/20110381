<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>All Blog Posts</title>
    <link rel="stylesheet" type="text/css" href="/stylesheets/styles.css">
    <style>
        body {
            background-color: #f0f8ff;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #007bff;
            color: #fff;
            text-align: center;
            padding: 5px 0;
            max-height: 10vh;
            height: 100%;
        }

        main {
            max-width: 80vw;
            margin: 0 auto;
            padding: 20px;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            background-color: #fff;
            margin-bottom: 20px;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        h2 {
            color: #007bff;
        }

        p {
            margin: 0;
        }

        a {
            text-decoration: none;
            color: #007bff;
        }

        a:hover {
            text-decoration: underline;
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

        footer {
            text-align: right;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <header>
        <h1>All Blog Posts</h1>
        <a href="/articles/create-post" style="position: absolute; top: 20px; right: 20px; color: #fff; text-decoration: none;">Create New Post</a>
    </header>
    
    <main>
        <ul>
            {{#each posts}}
            <li>
                <h2>
                    <a style="text-decoration: none;cursor: pointer;" href="/articles/{{ this.id }}">{{ this.title }}</a>
                </h2>
                <p>Author: {{ this.author }}</p>
                <a href="/articles/{{ this.id }}">Read More</a>
            </li>
            {{/each}}
        </ul>
    </main>
</body>
</html>
